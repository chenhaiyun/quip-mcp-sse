# Quip API 令牌（必需）
QUIP_TOKEN=your_quip_api_token_here

# Quip API 基础 URL（可选，默认为 https://platform.quip.com）
QUIP_BASE_URL=https://platform.quip.com

# 服务器端口（可选，默认为 8082）
PORT=8082

# CSV 文件的存储路径（可选，默认为 ./storage）
QUIP_STORAGE_PATH=./storage
